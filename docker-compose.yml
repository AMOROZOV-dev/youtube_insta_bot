version: "3.8"
services:
  bot:
    build: .
    container_name: tg_video_bot
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DOWNLOAD_DIR=/downloads
    volumes:
      - ./downloads:/downloads
    restart: unless-stopped
